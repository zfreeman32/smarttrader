# Priorities
- [ICT] Keep canonical ICT experiment documentation current in `docs\ICT_execution_journal.md`; each new ICT run should record the `what / why / result / observations / decision` story there, with the audit reports and design paper staying at the summary level.
- [FRVP] Keep canonical experiment documentation current in `docs\FRVP_experiment_journal.md`; each new FRVP run should record the `why / what / how / result / what worked / what didn't / observations / decision` story there, with the audit report and design paper staying at the summary level.
- [General] See `docs\FRVP_ICT_Live_Signal_Output_Audit_20260811.md` for the live signal-layer audit (why FRVP/ICT shadow predictions cross threshold far more often than OTE, per-branch AP-lift-over-base-rate table, and filtering tips). The items below tagged `(2026-08-11 audit)` come from that pass.

## First
- [x] [ICT] `(decided 2026-08-13)` Promote `ict_long_meta_xgb_v1` as the sole controlled paper-signal leader under the exact accepted global-`0.40`, no-abstain contract. Keep `ict_long_reversal_xgb_v1` candidate/shadow because 612 historical trades overlap long-meta, those shared trades contribute 105.2% of reversal net PnL, and reversal's 663 unique trades lose `286.95` ticks. Active-meta prediction parity passes after restoring the XGBoost null contract; its two residual raw-ID mismatches are zero-split dead inputs, while reversal's inversion-index mismatch has a small nonzero effect and requires a clean retrain. The accepted 20-completed-bar overlapping event-markout ledger is implemented with stop/target not applicable. Lifecycle is authorized pending a healthy authenticated paper feed; the four-week clock remains `not_started`. Active here permits signals/notifications, not broker orders or fills.
- [FRVP] Keep the July 28, 2026 deferred-research audit as the ordering contract: `model_testing\reports\frvp_deferred_research_audit\frvp_deferred_research_audit_20260728\DEFERRED_RESEARCH_SUMMARY.md`.
- [FRVP] Implement the per-setup-family FRVP lane first in code and artifact plumbing before any new FRVP retraining: split the current pooled FRVP targets through `data\labeling\frvp_labeling_engine.py`, `frvp\pipelines\es_primary_phase04.py`, `preprocessing\config.py`, and `preprocessing\pipeline.py`, then update the registry / reporting path as needed so setup-specific branches can be trained and evaluated cleanly.
- [ICT] `(2026-08-11 audit)` The same pooled-vs-family dilution pattern the FRVP split-first item above is trying to fix also shows up quantitatively on the ICT side: `ict_short_meta_xgb_v1` shares 284 exact walk-forward trades with `ict_short_reversal_xgb_v1` (66.5% / 85.1% of each branch's net PnL) and 84 trades with `ict_short_continuation_xgb_v1`. Treat the FRVP per-setup-family split as the template once it lands, and feed the result into the "pooled meta vs. family-specific models" comparison already queued under ICT backlog below.
- [General] `(2026-08-11 audit)` Populate and validate `abstain_policy.minimum_probability_quantile` — currently `null` on all 12 FRVP/ICT live `live_policy.json` contracts — for the branches with the weakest classifier discrimination: `frvp_short_meta_xgb_v1` (AP lift `0.014` over base rate), `frvp_long_meta_xgb_v1` (`0.067`), `ict_long_continuation_xgb_v1` (`0.009`), `ict_short_continuation_xgb_v1` (`0.059`). This is a different lever than the regime/session pocket-pruning already exhausted on FRVP (see "Recurring Lessons" in `docs\FRVP_experiment_journal.md`) — it filters on where a prediction sits in its own rolling probability distribution rather than on a fixed calibrated value, and it is already wired into the runtime abstain path, so it's a cheap experiment to run before spending more retraining effort on these four branches.
- [FRVP] Only after the per-setup-family code path exists, rerun the strongest long FRVP branches on the new setup-specific targets. Do not open another FRVP retrain branch before that code work lands, or it will create redundant training runs.
- [FRVP] Use that same per-setup path to run the standalone Setup 4 retest immediately after the split-first implementation, keeping Setup 4 separate from the pooled reversal family until the economics and sample-size read are explicit.
- [FRVP] After the split-first / Setup 4 code work, run the roll-translation vs. reset-at-roll A/B on the branches that still show roll-bracket drag, especially `frvp_long_continuation_xgb_v1` `v3` and the full-span long-reversal control.
- [FRVP] After those code-path changes are complete, decide whether `frvp_long_reversal_xgb_recent_regime_prune_v2` is already sufficient as the operational reversal contract or whether the repo still needs a new full-span train-side stability lever.
- [FRVP] Keep `Setup 6b` off the retrain queue until naked-VPOC context is promoted into FRVP event logic; the July 28, 2026 audit shows it is still not implemented in saved Phase 3 artifacts.
- [FRVP] Keep gamma-context features off the retrain queue until a real options-data contract exists; the July 28, 2026 audit shows gamma columns are still absent from the current Phase 2 feature contract.
- [FRVP] Run the extended shadow window from the `frvp_es_shadow_20260721` bundle after the next FRVP code / research changes that are actually intended to feed the next shadow package, focused on contract-switch behavior, dashboard continuity, signal persistence, and audit-replay consistency.
- [FRVP] Decide whether to complete the human same-contract TradingView profile signoff now or explicitly defer it; this is the main remaining non-ops gate before the ES-primary FRVP flow can be called complete.
- [FRVP] Write, review, and rehearse the manual IBKR front-month handoff runbook so the live roll procedure is explicit before the next contract switch.
- [ICT] Keep Phase 9 sequence-model work deferred until the shadow XGBoost leaders have been pressure-tested.
- [ICT] Leave the classic breaker version of Setup 4 deferred until the order-block state machine has a clean causal definition.

## After Individual Models Stabilize
- [ICT] Do cross-model risk / acceptance refinement only after the individual ICT / FRVP roster and promotion rules stabilize: position sizing, portfolio-level shadow-bundle evaluation, and a cleaner promotion contract belong here.
- [General] `(2026-08-11 audit)` As part of that cleaner promotion contract, add an explicit discrimination gate (minimum ROC AUC and/or minimum AP-lift-over-base-rate) alongside the existing Sharpe / drawdown / DSR / concentration gates. Right now a branch can clear every financial gate on a backtest while having close to zero real discriminative skill — e.g. `ict_long_continuation_xgb_v1` (AP lift `0.009`, ROC AUC `0.53`) and `frvp_short_meta_xgb_v1` (AP lift `0.014`, ROC AUC `0.53`) as of the 2026-08-11 signal-output audit. Compare against the OTE short-meta TCN champion (ROC AUC `0.98`, Brier `0.014`) as the "real skill" reference point.

## Parked / Conditional
- [ICT] Run a short-side regime-gated deployment / concentration study on the frozen `ict_es_primary_bootstrap_20260726_full` roster — the diagnosis is already done (`model_testing\reports\ict_contract_audits\ICT_SHORT_SIDE_CONCENTRATION_AUDIT_20260728.md`), this is the follow-up fix pass, analogous to the FRVP regime-gated deployment pass in `docs\FRVP_experiment_journal.md` E11. On the July 27, 2026 leakage-safe refresh, `ict_short_meta_xgb_v1` and `ict_short_reversal_xgb_v1` both miss the promotion-quality gate on single-trade concentration plus profitable-quarter breadth, and `284` exact shared events contribute `2597.4` ticks to each branch. Both branches also lean on shock days (`2020-03-17` is a top-3 winning day for both) — treat headline Sharpe/DSR on these two as inflated until performance excluding that date is checked.
- [ICT] Keep both continuation families below promotion priority for now: `ict_short_continuation_xgb_v1` is still concentration-dependent even after turning positive (`top trade share = 78.15%`), `ict_long_continuation_xgb_v1` is effectively flat (`+7.45` ticks net, ROC AUC `0.53`, essentially no discriminative skill), and `ict_short_meta_xgb_v1` now looks like a pooled short branch that is absorbing the profitable continuation subset.
- [ICT] Preserve and expand 1-minute coverage for ambiguous stop-vs.-target bars.
- [ICT] If selected-policy cadence is supposed to matter operationally, add an explicit model-level or bundle-level cadence gate; the current `min_trades_per_week` contract mostly constrains alternate policy selection and did not materially improve the roster when relaxed.
- [OTE] Keep short breakout shadow-only until the upstream event / target / trade-conversion layer is reworked and rerun end to end.
- [OTE] If breakout promotion becomes a priority again, reevaluate whether `long_breakout_tcn_champion` is strong enough to replace an active slot.
- [FRVP] Keep `frvp_long_reversal_xgb_v1` full-span recency `v3` frozen as the research control. Open a new train-side branch only if `frvp_long_reversal_xgb_recent_regime_prune_v2` proves operationally insufficient; do not reopen same-environment micro-pruning.
- [FRVP] Keep `frvp_long_meta_xgb_v1` v3 as a research checkpoint only; do not spend more time on micro-prunes unless Sharpe, concentration, or time-stability evidence changes materially.
- [FRVP] Keep short-side FRVP shadow-only research for now — this is an economics problem, not a filtering/threshold problem, so do not spend more threshold-tuning effort here until the underlying setup logic changes. `frvp_short_meta_xgb_v1` is still the cleanest short sentinel (Sharpe `~0`), but `frvp_short_continuation_tcn_v1` (Sharpe `-0.38`, max account drawdown `109.2%` — would blow the reference account) and `frvp_short_reversal_xgb_v1` (Sharpe `-0.39`, max account drawdown `48.7%`) both fail the drawdown gate outright under ES-aware costs.
- [ICT] Keep future ICT research in backlog: 1-minute execution refinement, barrier / exit study, setup-value study, event sparsity / trade-frequency study, optional FRVP confluence, and ES / NQ / 6E transfer work. (Pooled meta vs. family-specific models moved up to the "First" section above — it's now the ICT concentration audit's queued next-next step, not open-ended backlog.)
- [General] Keep broader future ideas parked: promotion-contract study, full ICT setup study, cross-strategy combination study, different model architectures after the policy-contract issues are fixed, HFT, and paper writeups.
